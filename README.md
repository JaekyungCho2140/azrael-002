# Azrael - L10n 일정 관리 도구

**Phase 0**: 영업일 역산 계산 및 시각화 (MVP)

엑셀 기반 L10n 일정 계산을 웹 기반 도구로 전환한 프로젝트입니다.

---

## 🎯 핵심 기능

- ✅ **영업일 역산 계산**: 업데이트일 기준 영업일 자동 계산 (주말/공휴일 제외)
- ✅ **3개 테이블 출력**: 일정표, Ext. 일정표, Int. 일정표
- ✅ **간트 차트 시각화**: Frappe Gantt 기반 3개 차트
- ✅ **캘린더 뷰**: FullCalendar 기반 월간 캘린더
- ✅ **이미지 복사**: html2canvas 기반 클립보드 복사 (Retina 2배 해상도)
- ✅ **프로젝트 관리**: 9개 기본 프로젝트 + 사용자 추가 프로젝트
- ✅ **공휴일 관리**: 공공데이터포털 API 연동 + 수동 추가
- ✅ **설정 관리**: 업무 단계, 공휴일 템플릿 관리

---

## 🏗️ 기술 스택

- **Frontend**: React 18 + TypeScript + Vite
- **Storage**: LocalStorage (서버 없음)
- **Charts**: Frappe Gantt v1.0.3, FullCalendar v6
- **Utils**: html2canvas v1.4.1
- **Testing**: Vitest + jsdom

---

## 🚀 시작하기

### 1. 의존성 설치

```bash
npm install
```

### 2. 환경 변수 설정

`.env` 파일을 생성하고 다음 내용을 추가하세요:

```env
# 개발 모드 (OAuth 인증 우회)
VITE_DEV_MODE=true

# 개발 모드용 테스트 이메일
VITE_DEV_EMAIL=dev@azrael.local

# 화이트리스트 (쉼표로 구분)
VITE_ALLOWED_USERS=dev@azrael.local,user1@company.com,user2@company.com

# 공휴일 API 키 (공공데이터포털)
VITE_HOLIDAY_API_KEY=your_api_key_here
```

**⚠️ 개발 모드**:
- `VITE_DEV_MODE=true`: 자동 로그인 (OAuth 우회)
- `VITE_DEV_MODE=false`: 실제 Gmail OAuth (추후 구현)

### 3. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 http://localhost:3000 접속

### 4. 프로덕션 빌드

```bash
npm run build
```

빌드 결과는 `dist/` 디렉토리에 생성됩니다.

---

## 📋 명령어

```bash
npm run dev        # 개발 서버 (http://localhost:3000)
npm run build      # 프로덕션 빌드
npm run preview    # 빌드 결과 미리보기
npm test           # 단위 테스트 (Vitest)
npm run typecheck  # TypeScript 타입 체크
```

---

## 🗂️ 프로젝트 구조

```
azrael-002/
├── src/
│   ├── components/     # UI 컴포넌트
│   │   ├── LoginScreen.tsx
│   │   ├── OnboardingScreen.tsx
│   │   ├── MainScreen.tsx
│   │   ├── ScheduleTable.tsx
│   │   ├── GanttChart.tsx
│   │   ├── CalendarView.tsx
│   │   ├── SettingsScreen.tsx
│   │   ├── Button.tsx
│   │   ├── Input.tsx
│   │   ├── EditableCell.tsx
│   │   ├── Toast.tsx
│   │   ├── Loading.tsx
│   │   └── ResolutionWarning.tsx
│   ├── hooks/          # React Hooks
│   │   ├── useProjects.ts
│   │   ├── useHolidays.ts
│   │   ├── useLocalStorage.ts
│   │   ├── useImageCopy.ts
│   │   └── useToast.ts
│   ├── lib/            # 순수 함수
│   │   ├── businessDays.ts     # 영업일 계산 엔진
│   │   └── storage.ts          # LocalStorage 유틸리티
│   ├── types/          # TypeScript 타입 정의
│   │   └── index.ts
│   ├── styles/         # CSS 스타일
│   │   ├── tokens.css
│   │   └── index.css
│   ├── test/           # 테스트 설정
│   │   └── setup.ts
│   ├── App.tsx
│   └── main.tsx
├── prd/                # 상세 PRD 문서
│   ├── Azrael-PRD-Master.md
│   ├── Azrael-PRD-Shared.md
│   ├── Azrael-PRD-Design.md
│   └── Azrael-PRD-Phase0.md
├── plan.md             # 개발 로드맵
├── CLAUDE.md           # 개발 가이드
├── .env.example        # 환경 변수 예제
└── package.json
```

---

## 📐 아키텍처 원칙

### 데이터 흐름

```
LocalStorage
    ↓
Hooks (useProjects, useHolidays)
    ↓
Components (MainScreen, SettingsScreen)
    ↓
Lib Functions (calculateBusinessDate, formatTableDate)
    ↓
Results (CalculationResult → Tables, Charts, Calendar)
```

### 핵심 규칙

- **lib/ 함수는 순수 함수**: LocalStorage 직접 접근 금지
- **Date 직렬화**: JSON.parse 후 Date 필드 수동 복원 필수
- **"마감" = startDateTime**: 종료일이 아님! (PRD 용어 주의)
- **Single Source of Truth**: ScheduleEntry.startDateTime/endDateTime만 관리

---

## 🧪 테스트

### 단위 테스트 실행

```bash
npm test
```

**커버리지** (15개 테스트):
- ✅ 영업일 역산 계산 (5개 시나리오)
- ✅ 날짜 형식 변환 (4개 시나리오)
- ✅ LocalStorage Date 직렬화 (3개 시나리오)
- ✅ 하위 일감 양방향 참조 (3개 시나리오)

### 수동 테스트 시나리오

1. **로그인** → 개발 모드: 자동 로그인 (0.5초 후)
2. **온보딩** → 프로젝트 선택 (예: M4/GL) → "시작하기"
3. **메인 화면** → 업데이트일 입력 (예: 2026-02-10) → [계산]
4. **결과 확인**:
   - 상단 날짜 (헤즈업, iOS 심사일)
   - 테이블 1/2/3 렌더링
   - 간트 차트 3개
   - 캘린더 뷰
5. **이미지 복사** → [📋 이미지 복사] 버튼 클릭 → 클립보드 확인
6. **테이블 편집** → "설명" 셀 클릭 → 텍스트 입력 → Bold/Italic 서식 적용 → 저장
7. **설정** → 프로젝트 관리, 업무 단계 추가, 공휴일 불러오기

---

## 🎨 디자인 시스템

### 색상 팔레트

- **Primary**: Azrael Orange (#FF9800)
- **테이블 1 차트**: Orange (#FF9800)
- **테이블 2 차트**: Teal (#009688)
- **테이블 3 차트**: Deep Purple (#673AB7)
- **중립**: Gray 50-900

### 타이포그래피

- **Display**: Nunito (제목, 로고)
- **Body**: Inter (본문, UI)
- **Korean**: Noto Sans KR (한글)
- **Mono**: SF Mono, Consolas (코드, 날짜)

---

## 🐛 알려진 제한사항

### Phase 0 범위

- **OAuth**: Gmail OAuth는 개발 모드로 우회 (실제 구현 미완)
- **JIRA 연동**: Phase 1에서 구현 예정
- **이메일 생성**: Phase 2에서 구현 예정
- **슬랙 연동**: Phase 3에서 구현 예정

### 브라우저 호환성

- **최소 해상도**: 1280x720
- **지원 브라우저**: Chrome, Edge, Firefox, Safari (최신 2개 버전)
- **Clipboard API**: Chrome/Edge 권장 (Safari는 제한적)

---

## 📚 참조 문서

- **개발 로드맵**: [plan.md](./plan.md)
- **개발 가이드**: [CLAUDE.md](./CLAUDE.md)
- **PRD 문서**: [prd/](./prd/)
  - [Master PRD](./prd/Azrael-PRD-Master.md)
  - [Shared Specs](./prd/Azrael-PRD-Shared.md)
  - [Design System](./prd/Azrael-PRD-Design.md)
  - [Phase 0 PRD](./prd/Azrael-PRD-Phase0.md)

---

## 🛠️ 개발 가이드

### 새 컴포넌트 추가

1. `src/components/MyComponent.tsx` 생성
2. `src/components/MyComponent.css` 생성
3. CSS 변수 (`--azrael-*`) 사용
4. PRD 참조 링크 주석 추가

### 새 계산 로직 추가

1. `src/lib/` 디렉토리에 순수 함수로 작성
2. LocalStorage 직접 접근 금지 (hooks를 통해서만)
3. 단위 테스트 작성 (`*.test.ts`)

### LocalStorage 키 추가

1. `src/types/index.ts`의 `STORAGE_KEYS` 업데이트
2. `src/lib/storage.ts`에 getter/setter 함수 추가
3. Date 필드가 있으면 `reviveEntries` 유사 함수 작성

---

## 📊 성능 지표

- **계산 성능**: < 1초 (영업일 역산 + ScheduleEntry 생성)
- **렌더링 성능**: < 1초 (3개 테이블 + 3개 차트 + 캘린더)
- **빌드 크기**:
  - JS: 589KB (gzip: 169KB)
  - CSS: 21KB (gzip: 4.68KB)
- **빌드 시간**: ~1.2초

---

## 🔐 보안 고려사항

**⚠️ 클라이언트 전용 앱의 한계**:
- `.env` 파일이 번들에 포함됨 (소스 코드 노출 가능)
- 화이트리스트가 클라이언트에서 검증됨 (우회 가능)
- 공휴일 API 키가 노출됨

**권장 사항**:
- Phase 1 이후 백엔드 추가 시 인증/권한 관리 서버 측으로 이동
- 내부 팀 전용 도구로 제한 (공개 배포 지양)

---

## 📄 라이선스

MIT License

---

## 👥 기여자

L10n팀 내부 프로젝트

---

**문서 버전**: 1.0
**마지막 업데이트**: 2026-01-12
